<%= render :partial => 'registrations/new'%>

<%= @subdomain.name %> <br />
<% @files.each do |f|%>
	<% if f[0] %> <!-- It's a directory -->
		<strong><%= f[1] %></strong><br />
	<% else %>
		<%= f[1] %><br />
	<% end %>
<% end %>